---
- name: Test sudo
  hosts: master
  become: yes
  tasks:

    - name: Update Sudoers
      lineinfile:
        path: /etc/sudoers
        regexp: '^ansible ALL= (newuser)'
        line: ansible ALL= (newuser) NOPASSWD: ALL